UQ [BizDev]/[workshop] version='0.1.1' author='henry' {
    SCHEDULE Test ON 6:31;
}

ENUM Role ( 
    counselor = 1,
    volunteer = 20,
);

IX *UserRole (
    ix User,
    xi TYPE Local,
);

ID PersonRole (
    id,
    KEY person ID Person,
    KEY role ENUM Role,
    INDEX role_person(role, person) UNIQUE,
);

PROC Test() {
    VAR a INT;
    SET a=1;
};
